FROM_SERVER_HOST=localhost
FROM_SERVER_PORT=5672
FROM_SERVER_EXCHANGE=exchange1
FROM_SERVER_ROUTING_KEY=rk1
FROM_SERVER_QUEUE=queue1

TO_SERVER_HOST=localhost
TO_SERVER_PORT=5672
TO_SERVER_EXCHANGE=exchange2
TO_SERVER_ROUTING_KEY=rk2